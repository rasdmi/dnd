// config.js
// Optional external GM endpoint (Cloudflare Worker / your server).
// If empty, game uses local GM stub (free, no AI).
export const GM_ENDPOINT = ""; // e.g. "https://your-worker.yourname.workers.dev/gm"
